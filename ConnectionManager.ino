ConnectionManager::ConnectionManager() {
  // init WiFi
}

bool ConnectionManager::connect(const char *ssid, const char *password) {
  // connect to Wifi
}

bool ConnectionManager::disconnect() {
  // disconnect to Wifi
}
